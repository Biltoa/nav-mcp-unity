# ---- .NET ----
bin/
obj/
*.user
*.suo
*.userosscache
*.sln.docstates
artifacts/
publish/
*.nupkg
.vs/
.idea/

# ---- Unity (package dev; scoped to unity/ so it cannot eat our .csproj/.sln) ----
unity/**/Library/
unity/**/Temp/
unity/**/Obj/
unity/**/Build/
unity/**/Builds/
unity/**/Logs/
unity/**/UserSettings/
unity/**/MemoryCaptures/
unity/**/*.csproj
unity/**/*.sln
*.unityproj
*.pidb
*.booproj
*.svd
*.apk
*.aab
*.unitypackage
sysinfo.txt

# ---- Runtime / local state ----
# The daemon writes to %LOCALAPPDATA%/UnityMCP, never into the repo,
# but keep these guards in case a run is misconfigured.
*.log
logs/
/token
*.token
local.settings.json
.env

# ---- Benchmarks / scratch ----
# bench-results/ IS committed: the measurements are the evidence for every claim in PROGRESS.md.
scratch/
*.tmp

# ---- OS ----
Thumbs.db
desktop.ini
.DS_Store

# ---- release drop ----
dist/
